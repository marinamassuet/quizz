<script
  let total = hggsp + ses + math + phys + hlp + amc + svt + nsi;
  let pmath = math / total * 100;
  let pphys = phys / total * 100;
  let phlp = hlp / total * 100;
  let phggsp = hggsp / total * 100;
  let pses = ses / total * 100;
  let pamc = amc / total * 100;
  let psvt = svt / total * 100;
  let pnsi = nsi / total * 100;
